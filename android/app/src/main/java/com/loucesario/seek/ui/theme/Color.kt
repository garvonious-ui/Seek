package com.loucesario.seek.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Color tokens mirrored from docs/design-system.md (Gold + Soft Sage palette).
 * Use these explicit values everywhere — never adaptive/system colors, matching
 * the iOS rule that the app is always light mode.
 */
object SeekColors {
    val Gold = Color(0xFFCDA349)
    val LightGold = Color(0xFFEDD99A)
    val Sage = Color(0xFF5B7B5E)
    val MidSage = Color(0xFF8AAF8D)
    val Background = Color(0xFFFAFAF6)
    val Surface = Color(0xFFFFFFFF)
    val TextPrimary = Color(0xFF1A1A1A)
    val TextSecondary = Color(0xFF6B7280)
    val TextTertiary = Color(0xFF9CA3AF)
    val Success = Color(0xFF4CAF50)
    val Warning = Color(0xFFF59E0B)
    val Divider = Color(0xFFE5E7EB)
    val SurfaceMuted = Color(0xFFF3F4F6)
}
